FOR CODEX:
Read and follow these workspace instruction files before doing anything:

AGENTS.md
BOOTSTRAP.md
skills/cblm_generator/SKILL.md
TOOLS.md
USER.md
SOUL.md
HEARTBEAT.md
IDENTITY.md

Assume this is a clean workspace with exactly one valid syllabus file in `./inbox`.

Now execute the repository workflow with these strict rules:

General rules:
- Work on only that one syllabus file.
- Do not ask me unnecessary questions.
- Prefer `.\.venv\Scripts\python.exe` for Python commands.
- Use up to 600000 ms timeout for long-running terminal commands.
- Use the modular templates in `./templates`.
- Use `tools/assemble_cblm.py` as the production assembler.
- Do not rely on the old monolithic `CBLM Template.docx` workflow unless explicitly told to use legacy mode.

Unit-by-unit workflow:
1. Read the syllabus from `./inbox`.
2. Extract qualification title, all units of competency, and the current unit's LOs, topics, and subtopics.
3. Build one structured payload JSON for the current unit only.
4. Save the draft payload JSON in `./state`.
5. Validate the draft payload completely before DOCX assembly.
6. Ensure every Key Facts section follows the repository requirements for length, structure, specificity, and distinctness.
7. Run:
   `.\.venv\Scripts\python.exe .\tools\check_keyfacts_similarity.py "<payload.json>"`
8. If redundancy is flagged, rewrite only the flagged Key Facts sections once, save the payload again, and rerun the checker.
9. If the second similarity check still fails for that unit, stop and treat the unit as failed according to the repository rules.
10. If validation passes, generate the DOCX for the current unit with:
    `.\.venv\Scripts\python.exe .\tools\assemble_cblm.py "<payload.json>" "<output.docx>"`
11. After one unit is successfully completed, pause and report status.
12. Wait for me to say `continue` before starting the next unit.
13. When I say `continue`, resume from the saved state and process the next unit instead of restarting from scratch.
14. Move the source file to `./processed` only after all required unit DOCX files are successfully completed.
15. If all units succeed, also create `./output/CBLM_FULL.docx`.

Quality rules:
- Do not lower quality requirements to finish faster.
- Do not use placeholder text.
- Do not shorten required Key Facts below spec.
- Make each unit materially different from the others in topic coverage, explanation, examples, and workplace application.
- Do not claim full success for the syllabus after finishing only one unit.

If you must pause, report:
- what unit was completed
- what files were written
- what remains
- whether it is safe to continue

Execute the first unit now.
